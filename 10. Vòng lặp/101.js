// Vòng lặp While

var i = 1;

while(i <= 1000) {
    // i++ // nếu để i++ ở đây sẽ trả về kết quả là i ở dòng 3 +1 khi bắt đầu chạy
    console.log(i);
    i++;
}

var languages= [
    'Javalorant',
    'HTMLMHT',
    'Cssgo'
];
var i = 1;

while(i <= languages.length) {
    console.log(languages[i]); // lấy ra các phần tử của mảng
    i++;
}
